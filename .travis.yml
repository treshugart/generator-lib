language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-lib' ]; then cd .. && eval "mv $currentfolder generator-lib" && cd generator-lib; fi

